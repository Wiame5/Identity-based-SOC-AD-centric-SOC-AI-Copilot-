# Development fix scripts

One-off patch scripts used during development to debug and iterate on the
pipeline (timezone handling, feature encoding, class weighting, NaN handling,
etc.). Each script's changes are already merged into the corresponding files
under `src/` — these are kept for transparency into the debugging process,
not meant to be re-run.

See the project report for the full narrative behind each fix.
